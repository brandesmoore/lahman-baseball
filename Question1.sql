/*Question 1*/

SELECT MIN(yearid) as min_year, MAX(yearid) AS max_year
FROM batting;

SELECT MIN(yearid) as min_year, MAX(yearid) AS max_year
FROM fielding;

SELECT MIN(yearid) as min_year, MAX(yearid) AS max_year
FROM pitching;